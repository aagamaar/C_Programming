#include <stdio.h>
int main()
{
    /*Inputting values from the user*/
    float a,b,sum;
    printf("Enter a :");
    scanf("%f",&a);
    printf("Enter b :");
    scanf("%f",&b);

    int sum = a + b;
    
}
#include <stdio.h>
int main()
{
    int a,b,sum=0;
    printf("Enter the value of a and b: ");
    scanf("%d %d",&a, &b);
    sum=a+b;
    printf("The sum of the 2 integers: %d", sum);
    return 0;
}